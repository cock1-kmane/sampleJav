# Language setting
language: java

# Version number
jdk:
  - openjdk6
  - openjdk7
  - oraclejdk7
  #- oraclejdk8

      
build:
  pre_ci_boot:
    image_name: drydock/u12jav
    image_tag: prod
    pull: true

  ci:
    - java -version
   # - if [ "$SHIPPABLE_JAVA_VERSION" == "oraclejdk7" ]; then foobar; fi
    - mkdir -p shippable/testresults
    - mkdir -p shippable/codecovarage

  after_success:
    - mvn clean cobertura:cobertura
    - mvn test
